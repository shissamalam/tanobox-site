# tanobox.com

Marketing and business plan site for TanoBox — self-hosted financial modeling for small AEC firms.

## Structure
- `/` → redirects to /businessplan
- `/businessplan` → full business plan landing page

## Deploy
Connected to Vercel. Push to `main` triggers auto-deploy.

## Local preview
Open `businessplan/index.html` in a browser — no build step required.
